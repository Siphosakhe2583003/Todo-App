import React, { useState } from 'react'

export default function Task({ id, task, handleOnDrag, type }) {
  return (
    <div className="task" key={id} draggable onDragStart={e => handleOnDrag(e, type, task) onMouseEnter={() => ({ func })}>
      {task}
    </div>
  )
}
